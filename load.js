const load = function(id, formula) {
	//console.log('loading '+id+' as default '+formula);
	return formula;
};
const save = function(id, formula) {
	//console.log('saving '+id+' as '+formula);
	return true;
}
